#include <stdio.h>

/**
* main - main block to be executed
* Description: printing base 10 digits
* Return: Always 0 (success)
*/

int main(void)
{
int n;
char ch;
for (ch = 48; ch <= 58; ch++)
{
putchar(n);
}
for (ch = 'a'; ch <= 'f'; ch++)
{
putchar(ch);
}
putchar('\n');
return (0);
}
